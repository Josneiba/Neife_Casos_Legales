# Valores ficticios solo para `prisma generate` / validate sin BD real.
# Copia credenciales reales a `.env` (no subir) o exporta vars antes de `db push` / `migrate`.
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/postgres"
DIRECT_URL="postgresql://postgres:postgres@127.0.0.1:5432/postgres"
